

class Intercept(object):
    #point = position
    def __init__(self, point, normal, distance, obj):
        self.point = point
        self.normal = normal
        self.distance = distance
        self.obj = obj